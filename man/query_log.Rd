% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query_log.R
\name{read_query_log}
\alias{read_query_log}
\alias{write_query_log}
\title{Read and Write the Query Log}
\usage{
read_query_log(
  log_file = read_secret("db.query_log_sqlite"),
  date_range = this_year(),
  with_joins = TRUE,
  joins_only = FALSE
)

write_query_log(
  log_file,
  user,
  source_type,
  source_dsn,
  source_project,
  source_cbase = NA_character_,
  query,
  rows,
  columns,
  duration_secs = NA_real_,
  print = TRUE
)
}
\arguments{
\item{log_file}{path to the SQLite query log file. Defaults to the path set in \code{read_secret("db.query_log_sqlite")}.}

\item{date_range}{date range to filter log entries on, can be length 1 or 2 (or more to use the min/max). Defaults to \code{\link[certetoolbox:days_around_today]{certetoolbox::this_year()}}. Use \code{NULL} to return all entries. Can also be years, or functions such as \code{\link[certetoolbox:days_around_today]{last_month()}}.}

\item{with_joins}{if \code{TRUE}, add the joins to the table}

\item{joins_only}{if \code{TRUE}, return only the joins table instead of the main query log}

\item{user}{user name that ran the query}

\item{source_type}{type of source, e.g. \code{"Diver"} or \code{"MySQL"}}

\item{source_dsn}{the DSN used to connect}

\item{source_project}{the project used to connect}

\item{source_cbase}{the cBase used to connect (if applicable)}

\item{query}{the SQL query that was run}

\item{rows}{number of rows returned}

\item{columns}{number of columns returned}

\item{duration_secs}{duration of the query in seconds}

\item{print}{a \link{logical} to indicate whether info should be printed}
}
\description{
The \code{\link[=read_query_log]{read_query_log()}} function reads the query log from a SQLite database. The \code{\link[=write_query_log]{write_query_log()}} function writes a query log entry. Join queries from presets are stored in a separate table and linked to their parent query; use \code{\link[=read_query_log]{read_query_log()}} to retrieve them together.
}
\examples{
\dontrun{

read_query_log() # this year (default)
read_query_log(date_range = 2024) # all of 2024
read_query_log(date_range = last_month()) # last month
read_query_log(date_range = c(2023, 2025)) # range of years
read_query_log(date_range = NULL) # everything
}
}
